import express from "express";
// import {
//   createDemande,
//   getDemande,
//   getAllDemandes,
//   respondToDemande,
//   deleteDemande,
// } from "../controllers/demande.js";
// import { verifyToken } from "../middleware/auth.js";

const router = express.Router();
// // @POST     "/demande/"
// // @desc    Create a new Demande
// router.post("/", verifyToken, createDemande);

// // @GET     "/demande/"
// // @desc    Get all categories
// router.get("/", verifyToken, getAllDemandes);

// // @GET     "/demande/:id"
// // @desc    Get Demande by id
// router.get("/:id", verifyToken, getDemande);

// // @PUT     "/demande/:id"
// // @desc    Respond to a demande by id
// router.get("/:id", verifyToken, respondToDemande);

// // @DELETE  "/demande/:id"
// // @desc    Delete a demande by id
// router.get("/:id", verifyToken, deleteDemande);

export default router;
